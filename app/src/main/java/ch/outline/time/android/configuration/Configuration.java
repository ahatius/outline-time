package ch.outline.time.android.configuration;

/**
 * Created by Ahatius on 24.01.2016.
 */
public class Configuration {
    public static final String LOGIN_URL = ""; // URL to call for the login
    public static final String TOKEN_URL = ""; // Timestamp parameter should be at the end so the timestamp can be appended
    public static final String INSERT_URL = ""; // URL to insert time, Timestamp parameter should be at the end
}